import { jar, client } from "./client";
import { makeDefaults } from "./defaults";
import { cleanGet, get, post, postFormData } from "./methods";
import { setProxy } from "./proxy";

export {
  cleanGet,
  get,
  post,
  postFormData,
  jar,
  setProxy,
  makeDefaults,
  client
};
